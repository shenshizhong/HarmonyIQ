

```
三方SDK 通常由静态库和适配代码构成。

SDK的业务逻辑通过硬件模组工具链 编译到静态库libs，每款模组都有对应的libs。

三方sdk目录如下：
1、适配代码 adapter，放置到domains/io/link/目录下，与模组解耦
2、业务库libs，放置到device/hisilicon/hispark_pegasus/sdk_liteos/3rd_sdk/ 目录下，与模组绑定


步骤：
1、创建厂商目录，domains/iot/link/demolink、
device/hisilicon/hispark_pegasus/sdk_liteos/3rd_sdk/，用于厂商隔离

2、创建domains/iot/link/demolink/BUILD.gn,用于构建适配代码
3、创建device/hisilicon/hispark_pegasus/sdk_liteos/3rd_sdk/demolink/libs/ 目录，用于存放业务库libs

构建业务libs


创建线程接口

查看接口文档，选取一个功能类似的接口，

脚本编写：

```