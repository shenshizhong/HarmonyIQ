


```
传感器服务层： 提供对上层应用的接口
传感器管理框架层： 管理传感器设备和服务
HDF驱动框架层：硬件驱动框架
传感器驱动层：具体传感器设备的驱动实现

1、创建驱动模块
2、编写驱动代码
传感器驱动基本结构
#include "hdf_device_desc.h"
#include "sensor_platform_if.h"

struct MySensorOps{
    int32_t(*Init)(struct SensorDeviceInfo *deviceInfo);
    int32_t(*Enable)(void);
    
}


```

扫地机器人 3400
空调 5000
沙发 8000
茶几 400
