

主机的文件共享到虚拟机的ubuntu中
```
一、
1、关闭ubuntu
2、vmware 点击虚拟机-> 设置 -> 选项-> 共享文件
3、总是启用，然后添加主机上的文件夹（D:\share）

二、

在ubuntu 的 Other Locations -> Computer 共享文件夹通常位于/mnt/hgfs/下
1、手动挂载
sudo vmhgfs-fuse .host:/ /mnt/hgfs -o allow_other

2、检查是否有共享的文件
ls /mnt/hgfs/share
```