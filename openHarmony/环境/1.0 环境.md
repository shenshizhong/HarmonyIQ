

```
交叉编译工具链：确保Host系统（如 Ubuntu）的架构(x86_64) 与 Target（如ARM）匹配。
驱动调试工具：
  Linux端：gdb、kgdb、JTAG 调试器
  Windows端：串口工具（SecureCRT、MobaXterm）
  
文件共享：

BIOS (Basic Input/Output System)基本的输入输出系统
是计算机启动时运行的固件程序，位于主板的一块芯片。

BIOS核心作用：
1、硬件初始化
   开机时检测和初始化CPU、内存、硬盘、显卡等关键硬件
   通过POST（Power-On Self-Test）加电自检硬件是否正常。
   
2、启动引导
   按照
    
```

```
在鸿蒙系统重，南向开发包括：
硬件抽象层（HAL）开发
内核移植
设备驱动开发
芯片适配（Hi3861、Hi3516）

目的是让鸿蒙OS 能运行在不同硬件平台上

交叉编译：
在计算机环境（x86 PC）上编译生成另一种（如 ARM芯片）可执行代码




环境安装：

宿主至少32G，虚拟机32G，处理器8核，硬盘ssd硬盘 800G，

虚拟机VmWare  快照  分配800G内存空间

ubuntu 推荐20版本，python 3.8

安装依赖库

创建工程并获取源码  -> 基于命令行开发 -> 安装库和工具集






```