

MS901M 加速度传感器驱动
```
MS901M 是一款多功能方向传感器

基于HDF 框架开发，使用标准的加速度传感器驱动接口

hcs:  Hareware Configuration Source 硬件配置描述文件
相当于Linux 设备树，dts

hcs作用：
将硬件配置（GPIO，中断号，寄存器地址）与驱动代码分离，提高可移植性。
修改硬件参数时无需重新编译驱动代码 ，只需要调整hcs文件

hcs 文件特点：
采用键值对和层级结构
支持#include 引用其他hcs文件

```