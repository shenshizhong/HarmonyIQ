
USB (Universal Serial Bus) 通用串行总线
```
硬件接口标准
用途： 设备之间的数据传输、充电、通信。

#include "hdf_usb.h"

//USB 驱动入口
static int32_t UsbDriverBind(struct HdfDeviceObject *device){
    HDF_LOGI("USB driver bind success.");
    return HDF_SUCCESS;
}

//初始化USB控制器
static int32_t UsbDriverInit(struct HdfDeviceObject *device){
    //硬件初始化代码
    return HDF_SUCCESS;
}

//注册到HDF 框架
struct HdfDriverEntry g_usbDriverEntry = {
    .moduleVersion = 1,
    .Bind = UsbDriverBind,
    .Init = UsbDriverInit,
    .moduleName = "usb_driver",
};
HDF_INIT(g_usbDriverEntry);


```