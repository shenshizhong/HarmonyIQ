

ARM 内核驱动移植
```
1、寄存器访问适配
//原x86 代码可能使用in/out指令
unsigned char inb(unsigned shor port);
void outb(unsigned char data, unsigned short port);

//ARM平台改为内存映射访问
#define REG_ACCESS(addr) (*(volatile unit32_t *) (addr))

2、中断处理适配

```